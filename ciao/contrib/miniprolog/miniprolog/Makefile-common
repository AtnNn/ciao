# -*- mode: Makefile; -*-

# Generate here all files that are architecture-independent

include $(SRCDIR)/SHARED

.PHONY: common

VPATH +=:$(SRCDIR)/bin/$(MPARCH)


common:: $(CONFIGOUT)

$(CONFIGOUT): $(CONFIGURE)
	$(CONFIGURE) $@
