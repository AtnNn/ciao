# -*- mode: Makefile; -*-

SRC=$(CIAODESRC)

# We have to fix this: DONT USE -include !!! --EMM

-include ../../../../../SETTINGS
-include ../../../../../SHARED

CONFIGURE=$(SRCDIR)/bin/_$(MPARCH)/configure/configure$(EXECSUFFIX)
WAM=$(SRCDIR)/bin/_$(MPARCH)/wam/wam$(EXECSUFFIX)
PROFVIEW=$(SRCDIR)/bin/_$(MPARCH)/profview/profview$(EXECSUFFIX)
PROFREPORT=$(SRCDIR)/bin/_$(MPARCH)/profreport/profreport$(EXECSUFFIX)
# CIAOC=ciaoc
