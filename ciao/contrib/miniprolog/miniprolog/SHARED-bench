# -*- mode: Makefile; -*-

include $(SRCDIR)/SHARED

TESTSRCDIR=$(SRCDIR)/tests
COMMONDIR=$(SRCDIR)/common/_all

WAMCMD=$(WAM) -t -s -C `$(CMDPATH) $(TESTSRCDIR)/` -C `$(CMDPATH)	\
	$(TESTSRCDIR)/calibs/` -C `$(CMDPATH) $(COMMONDIR)/`

CONFIGOUT=Configure.h EngineLoop.h InstructionBC.h Configure.cpp	\
	  miniprolog_auto.pl

# LOGFILE=$@.log
LOGFILE=/dev/null

-include $(SRCDIR)/SHARED-tests
