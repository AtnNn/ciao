#!/bin/sh
INSTENGINE="/home/clip/lib/ciao/ciao-1.3/engine/ciaoengine"${CIAOARCH:+".$CIAOARCH"}
ENGINE=${CIAOENGINE:-${INSTENGINE}}
LOAD_CIAO_LIB=${CIAOLIB:+"-L $CIAOLIB"}
exec $ENGINE "$@" -C -b $0 $LOAD_CIAO_LIB

